def solution(n):
    answer = ''
    while n:
        if n % 3:
            n, a = divmod(n, 3)
            answer += str(a)
        else:
            answer += '4'
            n = n // 3 - 1 
    return answer[::-1]